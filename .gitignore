node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Terraform
infra/.terraform/
*.tfstate
*.tfstate.*
tfplan

# Lambda: build generado por scripts/build-lambda.sh. La fuente de verdad es
# controller/, no una copia versionada que se desincroniza en silencio.
lambda/consult/vendor/
lambda/*.zip

# Secretos de la prueba manual (scripts/probar-consult.mjs)
scripts/.prueba.env
